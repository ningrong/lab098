(ns sandbox.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))


(defn -factor
(for [x (range):while (< x 100)
	 [x]]

cd lab098/sandbox/
git commit 
git push